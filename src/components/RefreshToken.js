import React, { Component } from 'react';

class RefreshToken extends Component {
    render = () => {
        return <div>
            <h1>Refresh Token</h1>
        </div>
    }
}

export default RefreshToken;